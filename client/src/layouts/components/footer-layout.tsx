import { memo } from 'react';

const FooterLayout = () => {
	return (
		<footer className="grid 2xl:grid-cols-4 h-fit gap-10 lg:grid-cols-3 bg-primary text-white py-10 px-px80">
			FooterLayout
		</footer>
	);
};

export default memo(FooterLayout);
